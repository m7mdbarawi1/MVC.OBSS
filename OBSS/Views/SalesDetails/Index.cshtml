@model IEnumerable<OBSS.Models.SalesDetail>
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer

@{
    ViewData["Title"] = Localizer["SalesDetails"];
}

<div class="container-fluid page-wrapper mt-4">
    <!-- Header -->
    <div class="row mb-4">
        <div class="col-12 d-flex flex-column flex-md-row justify-content-between align-items-md-center">
            <h1 class="page-title mb-3 mb-md-0">@Localizer["SalesDetails"]</h1>
            <div class="d-flex gap-2">
                <a asp-action="DownloadReport" class="btn btn-secondary">
                    <i class="bi bi-download"></i> @Localizer["DownloadReport"]
                </a>
            </div>
        </div>
    </div>

    <!-- Search -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-body">
                    <input type="text" id="searchInput" class="form-control" placeholder="@Localizer["SearchPlaceholder"]">
                </div>
            </div>
        </div>
    </div>

    <!-- Table -->
    <div class="row">
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead>
                                <tr>
                                    <th>@Localizer["Quantity"]</th>
                                    <th>@Localizer["Price"]</th>
                                    <th>@Localizer["Book"]</th>
                                    <th>@Localizer["Sale"]</th>
                                    <th class="text-center">@Localizer["Actions"]</th>
                                </tr>
                            </thead>
                            <tbody id="salesDetailsTable">
                                @if (Model != null && Model.Any())
                                {
                                    foreach (var item in Model)
                                    {
                                        <tr class="table-row-hover">
                                            <td>@item.Quantity</td>
                                            <td>$@item.Price</td>
                                            <td>@item.Book?.BookId</td>
                                            <td>@item.Sale?.SaleId</td>
                                            <td class="text-center">
                                                <div class="d-flex flex-wrap justify-content-center gap-2">
                                                    <a asp-action="Details" asp-route-id="@item.DetailId" class="btn btn-accent btn-sm">
                                                        <i class="bi bi-info-circle"></i> @Localizer["Details"]
                                                    </a>
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="5" class="text-center text-muted">@Localizer["NoRecordsFound"]</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const searchInput = document.getElementById("searchInput");
            const rows = document.querySelectorAll("#salesDetailsTable tr");

            searchInput.addEventListener("input", function () {
                const term = searchInput.value.toLowerCase();
                rows.forEach(row => {
                    const text = row.textContent.toLowerCase();
                    row.style.display = text.includes(term) ? "" : "none";
                });
            });
        });
    </script>
}
